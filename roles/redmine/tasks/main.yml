# descarga de Redmine

- name: Descargar Redmine
  ansible.builtin.unarchive:
    src: "https://www.redmine.org/releases/redmine-{{ redmine_version }}.zip"
    dest: /opt
    remote_src: true
    creates: "{{ redmine_root }}"

- name: Renombrar redmine descargado
  ansible.builtin.command: mv /opt/redmine-{{ redmine_version }} {{ redmine_root }}
  args:
    creates: "{{ redmine_root }}"

- name: Cambiar permisos del redmine_root
  ansible.builtin.file:
    path: "{{ redmine_root }}"
    owner: "{{ redmine_user }}"
    group: "{{ redmine_group }}"
    recurse: true

- name: Corregir permisos en .asdf antes de bundle # agregado por docker
  ansible.builtin.file:
    path: /home/redmine/.asdf
    state: directory
    owner: "{{ redmine_user }}"
    group: "{{ redmine_user }}"
    recurse: true

- name: Crear y dar permisos a /opt/.bundle para redmine # agregado por docker
  ansible.builtin.file:
    path: /opt/.bundle
    state: directory
    owner: "{{ redmine_user }}"
    group: "{{ redmine_user }}"
    mode: "0755"

# Configuracion Redmine

# - name: Añadir Puma al Gemfile.local
#  ansible.builtin.copy:
#    dest: "{{ redmine_root }}/Gemfile.local"
#    content: "gem 'puma'\n"
#    owner: "{{ redmine_user }}"
#    group: "{{ redmine_group }}"
#    mode: '0644'

- name: Añadir gemas de OpenTelemetry al Gemfile.local
  ansible.builtin.copy:
    dest: "{{ redmine_root }}/Gemfile.local"
    content: |
      gem 'puma'
      gem 'tzinfo-data'
      
      # OpenTelemetry instrumentation
      gem 'opentelemetry-sdk'
      gem 'opentelemetry-exporter-otlp'
      gem 'opentelemetry-instrumentation-all'
      gem 'opentelemetry-instrumentation-rails'
      gem 'opentelemetry-instrumentation-active_record'
      gem 'opentelemetry-instrumentation-rack'
      gem 'opentelemetry-instrumentation-net_http'
      gem 'opentelemetry-semantic_conventions'
    owner: "{{ redmine_user }}"
    group: "{{ redmine_group }}"
    mode: '0644'

- name: Generar archivo database.yml
  ansible.builtin.template:
    src: database.yml.j2
    dest: "{{ redmine_root }}/config/database.yml"
    owner: "{{ redmine_user }}"
    group: "{{ redmine_group }}"
    mode: '0640'

- name: Crear configuración de OpenTelemetry para Redmine
  ansible.builtin.template:
    src: opentelemetry.rb.j2
    dest: "{{ redmine_root }}/config/initializers/opentelemetry.rb"
    owner: "{{ redmine_user }}"
    group: "{{ redmine_group }}"
    mode: '0644'

- name: Crear archivo de variables de entorno para OpenTelemetry
  ansible.builtin.template:
    src: env.j2
    dest: "{{ redmine_root }}/.env"
    owner: "{{ redmine_user }}"
    group: "{{ redmine_group }}"
    mode: '0640'
    
# Quizas sea mala practica agrupar tanto, pero sino tenia problemas..
- name: Instalar dependencias y configurar Redmine
  become: true
  become_user: "{{ redmine_user }}"
  ansible.builtin.shell: |
    /bin/bash -lc '
      # Configurar bundle
      bundle config set --local without "development test"
      bundle install

      # Generar secret token
      bundle exec rake generate_secret_token

      # Ejecutar migraciones
      bundle exec rake db:migrate RAILS_ENV=production

      # Cargar datos por defecto
      REDMINE_LANG=es bundle exec rake redmine:load_default_data RAILS_ENV=production
    '
  args:
    chdir: "{{ redmine_root }}"
  changed_when: false

- name: Quitar permisos de ejecución en archivos de redmine
  ansible.builtin.shell: |
    find files log tmp public/assets -type f -exec chmod -x {} +
  args:
    chdir: "{{ redmine_root }}"
  changed_when: false

# Configuracion Redmine systemd

- name: Establecer config.public_file_server.enabled en false dentro de production.rb
  ansible.builtin.replace:
    path: "{{ redmine_root }}/config/environments/production.rb"
    regexp: '^\s*#?\s*config\.public_file_server\.enabled\s*=.*'
    replace: '  config.public_file_server.enabled = false'

- name: Instalar servicio systemd redmine
  ansible.builtin.template:
    src: redmine.service.j2
    dest: /etc/systemd/system/redmine.service
    owner: root
    group: root
    mode: '0644'

- name: Recargar systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Habilitar y arrancar el servicio redmine
  ansible.builtin.systemd:
    name: redmine.service
    enabled: true
    state: started
